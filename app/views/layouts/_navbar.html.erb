<header class="mb-4">
    <nav class="navbar navbar-expand-sm navbar-dark bg-success">
        <a class="navbar-brand" href="/">タスク管理一覧</a>
        
        <button class="navbar-toggler" type="button" data-toggle="collapce" data-target="#navbarSupportedContent">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class"collapce navbar-collapce" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto"></ul>
            <ul class="navbar-nav">
                <% if logged_in? %>
                  <li class="nav-item dropdown">
                      <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown"><%=current_user.name %></a>
                    <ul class="dropdown-menu dropdown-menu-right">
                      <li class="dropdown-item"><%= link_to 'ログアウトする', logout_path, method: :delete %></li>
                    </ul>
                  </li>

                <% else %>
                
                  <li class="nav-item"><%= link_to '新規登録', singup_path, class: 'nav-link' %></li>
                  <li class="nav-item"><%= link_to 'ログインの方はこちらへ', login_path, class: 'nav-link' %></li>
                
                <% end %>
            </ul>
        </div>
    </nav>
</header>